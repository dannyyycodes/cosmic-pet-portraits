You are improving src/components/funnel-v2/ReadingsLanding.tsx in a React + Vite + Tailwind site. You have VISION and image generation. Use them in a loop.

Read these first:
- .design-ref/image-shotlist.txt  (the art-director shot list for the 6 photoreal images this page needs)
- .design-ref/pawtraits-live.png  (the attached image, the visual quality bar)
- src/components/funnel-v2/ReadingsLanding.tsx  (the page you are fixing)
- src/components/portraits/tokens.ts  (PALETTE: cream, rose #bf524a, gold, ink)

THE PROBLEM (owner feedback, fix all of it):
1. The current generated images SCREAM AI and look low quality and fake. They have no force or feeling.
2. The checkout section still sits on an off background that looks bad.
3. The warm rose/heart background is only a tiny radial patch. The soft warm background should be the consistent background of the WHOLE page, subtle, not a small blob.
4. The AstrologyCredibility section is dark cosmic and BREAKS the cream/rose/gold colorway of the rest of the page. Restyle that authority section (or wrap it) so it fits the light cream/rose/gold palette while keeping all its real content: VSOP87, calculated to the minute, 13 celestial bodies, Real Astronomy Not Guesswork. Do NOT delete the authority, just make it match.
5. Overall quality is low. Raise it to match the pawtraits page density and polish.

DO THIS:
1. GENERATE 6 PHOTOREAL images, one per slot in image-shotlist.txt. They MUST look like real photographs: real homes, real pets, real human hands, real devices, golden-hour natural light, 35mm to 85mm lens look, warm cream and rose and gold grade. NO cosmic nebula pets, NO fake watercolour, NO glowing AI art.
2. CRITICAL VISION LOOP: after generating each image, OPEN it and look at it with your vision. If it looks AI generated, fake, plastic, has mangled paws or hands, or looks like art instead of a photo, DISCARD it and regenerate with a more photoreal prompt. Only keep images that genuinely look like a real professional photograph. Repeat until believable.
3. Write finals to public/readings/ and wire them into ReadingsLanding.tsx with width, height, lazy loading. Replace the existing weak images.
4. Apply a subtle warm cream-to-rose page-wide background behind the whole page.
5. Restyle the authority section to the light palette.
6. Keep the existing InlineCheckout exactly as imported and used. Do not touch checkout logic, only the section background around it.
7. COPY RULES strict: NO em-dashes. NEVER the words AI or report. Frame as a soul reading and a transformation. Do NOT invent review counts or star numbers.
8. Run: npx tsc -p tsconfig.app.json --noEmit and fix any type errors in files you changed.

At the end, summarize what you changed and list every generated image path. Report how many images you regenerated because the first attempt looked too AI.
