# BRIEF — Step 2 image redo (continuity with Step 1)

Use image generation (gpt-image-1), **transparent background**, ONE file:
`.assetstaging/howitworks2/step2.png` (square ≈1024×1024, real PNG alpha).

## Match Step 1 exactly
Open `.assetstaging/howitworks2/step1.png` (Step 1) AND
`.assetstaging/ref/cockapoo.jpg` (Monty). Step 2 must be visually CONTINUOUS
with Step 1: the SAME human hand, the SAME smartphone, and on the phone screen
the SAME candid photo of Monty as in Step 1 (same crop, same pose — apricot
red-gold cockapoo, curly coat, dark eyes, black nose, floppy ears). Same style,
lighting, scale and framing as Step 1 so they read as the same phone moment.

## The change vs Step 1
Now it's the "describe how you want them" step. On the phone screen, BELOW the
Monty photo, show a simple **prompt text input field** and a clean on-screen
**keyboard with a clearly visible spacebar**, and pose the hand/thumb as if
**typing** into it (thumb on the keyboard). It should clearly look like the
person is typing a prompt describing how they want the portrait. No green tick
(that was Step 1's "uploaded" cue).

## HARD RULES
- Fully TRANSPARENT background — no white/cream/scene/panel/card. Only the
  hand + phone (+ their soft contact shadow) opaque.
- NO real text/words/letters/numbers anywhere — the prompt field and keys are
  abstract shapes only (a soft cursor, blank key shapes, a wide blank
  spacebar). No readable characters.
- Monty on the screen must look like the SAME dog/photo as Step 1.
- Same warm boutique illustration style as Step 1. No watermark, no logo, no
  human face.

Save to the exact path, confirm true alpha, report path + size.
